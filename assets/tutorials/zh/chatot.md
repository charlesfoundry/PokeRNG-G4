# Pt/HGSS聒噪鸟与目标帧

## 核心规则

聒噪鸟每响起一声自制录音的叫声，就会消耗一次乱数。

这个消耗用于决定下一帧的音高。

每听到一声自制录音后，当前帧已经前进 **1 帧**。

## 和宝可梦生成的区别

宝可梦生成与聒噪鸟不同。

遇敌或对话触发生成时，游戏读取的是 **当前帧**，不是下一帧。

所以操作目标是：

1. 用喋喋不休直接推进到目标帧。
2. 再触发遇敌或按 A 生成宝可梦。

## 实机判断

app 会显示接下来每一帧的音高。

听到对应次数后，按照 app 的提示回到地图或目标界面，执行目标操作。
